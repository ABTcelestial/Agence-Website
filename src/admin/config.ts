// Admin panel routes (secret path) — all 'use client', not indexed
// Routes defined in: src/app/xn-ctrl-8z/ (matches NEXT_PUBLIC_ADMIN_PATH)

export const ADMIN_PATH = process.env.NEXT_PUBLIC_ADMIN_PATH ?? "xn-ctrl-8z";
